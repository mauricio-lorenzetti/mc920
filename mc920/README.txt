Pode ser necessária a instalação das bibliotecas NumPy e OpenCV, para tal:
$ pip install opencv-python
$ pip install numpy

Para executar o programa:
$ python3 main.py

Os arquivos serão gerados na pasta images/

